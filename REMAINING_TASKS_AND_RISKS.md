# 📋 KENKOU SYUTOKU 残存課題・潜在リスク・改善ロードマップ

本ドキュメントは、初期バグ修正（`datetime`インポート、骨塩量キー不一致、設定パス、Gemini 3.7 Flashモデル名更新）の完了後に残されている**潜在的リスク・未実装機能・保守性改善タスク**をまとめた一覧です。

---

## 📌 課題一覧サマリー

| No | 重要度 | 区分 | 対象ファイル / 領域 | 課題概要 |
|:---:|:---:|:---:|:---|:---|
| 1 | 🟠 **High** | 動作リスク | `src/calendar_picker.py` | ストレス取得用カレンダー座標の月跨ぎ・年跨ぎハードコード |
| 2 | 🟠 **High** | 堅牢性 | `src/adb_collector.py` | 体組成取得後の画面復帰（バックキー固定3回）の不安定性 |
| 3 | 🟡 **Medium** | 未実装 | `src/adb_collector.py`<br>`main.py` | `spo2`（血中酸素）の自動ADB画面キャプチャ機能の未実装 |
| 4 | 🟡 **Medium** | データ精度 | `src/data_extractor.py` | 体組成画像ペアリング判定条件の曖昧さ（誤マッチリスク） |
| 5 | 🟡 **Medium** | パフォーマンス | `src/report_generator.py` | JSON全件再走査による処理効率低下（データ肥大化リスク） |
| 6 | 🔵 **Low** | 保守性 | ルートディレクトリ | 60個以上のテスト・検証スクリプトの散在（整理・リファクタリング） |
| 7 | ✅ **完了** | 設定管理 | `config.py` / `.env` | Obsidian出力先の環境変数化・個人環境依存パスの完全排除（対応済） |

---

## 🔍 各課題の詳細と推奨対応方針

### 1. 【High】カレンダー座標の月跨ぎ・年跨ぎハードコード
- **対象ファイル**: [`src/calendar_picker.py`](file:///c:/KENKOU%20SYUTOKU/src/calendar_picker.py)
- **現状の仕様**:
  - `BASE_MONTH_ROW0_Y = 1517` は **2026年8月時点の実機画面（1600x2560）** に基づいて固定されています。
  - `HealthCalendarPicker.get_date_coords()` は指定日の「第何週目か (row)」を計算してY座標を算出しますが、月送り（8月から7月、または9月など前月/翌月）のUI操作（月切り替えタップ）に対応していません。
- **リスク**:
  - 月初（例: 9月1日〜9月7日）に過去7日分のストレスデータを取得しようとすると、前月（8月末）の日付がカレンダーに表示されていない/位置が異なり、**誤った日付をタップする、またはカレンダー外をタップして失敗する**。
- **推奨対応**:
  1. 対象日付が「表示中の月」と異なる場合の月送りタップ（`<` `>` ボタン）の自動化。
  2. カレンダーダイアログのヘッダー年月のOCR認識または現在月からの差分月数に応じたスワイプ・ナビゲーションの追加。

---

### 2. 【High】体組成取得後の画面復帰（固定3回バックキー）の不安定性
- **対象ファイル**: [`src/adb_collector.py` L280-L283](file:///c:/KENKOU%20SYUTOKU/src/adb_collector.py#L280-L283)
- **現状の仕様**:
  ```python
  for _ in range(3):
      self._run_adb(["shell", "input", "keyevent", "4"])
      time.sleep(1)
  ```
- **リスク**:
  - 過去日取得時に履歴一覧や詳細画面の深さが変わったり、通信遅延で画面遷移が完了していない場合、3回のバックキーでは「トップ画面まで戻りきらない」または「戻りすぎてアプリがバックグラウンド化・終了する」リスクがあります。
  - 次のカテゴリ（心機能・ストレス等）の処理開始時にトップ画面にいない場合、以降のタップ操作がすべて空振りになります。
- **推奨対応**:
  - トップ画面に戻る共通処理として、固定バックキー連打ではなく、`back_to_top_gesture()`（右端スワイプ）の併用、またはトップ画面の特定アイコン（「ヘルスケア」タブ等）のタップ、最悪時はアプリを `launch_app()` で再起動して確実にトップ状態を保証する。

---

### 3. 【Medium】血中酸素（SpO2）のADB自動収集未実装
- **対象ファイル**: [`src/adb_collector.py`](file:///c:/KENKOU%20SYUTOKU/src/adb_collector.py), [`main.py`](file:///c:/KENKOU%20SYUTOKU/main.py)
- **現状の仕様**:
  - `config.CATEGORIES` には `"spo2"` が定義され、`data_extractor.py` や `report_generator.py` にも SpO2 の解析・レポートロジックが存在します。
  - しかし `ADBCollector` に `collect_spo2()` メソッドが実装されておらず、`main.py --category spo2` や `collect_all_categories()` でも SpO2 は取得対象に含まれていません。
- **影響**:
  - SpO2 のデータを蓄積・分析するには、現状手動でスクリーンショットを配置する必要がある。
- **推奨対応**:
  - トップ画面の SpO2 カード（`CARD_COORDS["spo2"] = (1150, 1650)`）をタップし、睡眠/心拍と同様に日別スワイプ＋キャプチャを行う `collect_spo2(days=...)` の実装。

---

### 4. 【Medium】体組成画像ペアリング判定の曖昧さ
- **対象ファイル**: [`src/data_extractor.py` L320](file:///c:/KENKOU%20SYUTOKU/src/data_extractor.py#L320)
- **現状の仕様**:
  ```python
  if "body_composition" in name or "body_" in name:
  ```
- **リスク**:
  - `body_` を含む任意のテスト画像や別名保存画像が存在する場合、それらが意図せず体組成データとしてグループ化・マージ処理され、エラーやデータ汚染を引き起こす可能性があります。
- **推奨対応**:
  - 正規表現パターン（例: `r"^body_composition_\d{8}_(top|bottom)\.png$"`）で厳密にマッチングを行う。

---

### 5. 【Medium】データ量増加に伴う集計パフォーマンス（JSON全件再走査）
- **対象ファイル**: [`src/report_generator.py` L18-L36](file:///c:/KENKOU%20SYUTOKU/src/report_generator.py#L18-L36)
- **現状の仕様**:
  - レポート生成のたびに `data/json` 以下のすべての JSON ファイル（数百〜数千件）を再帰的に全走査・ロード・パースしています。
- **リスク**:
  - 運用が数ヶ月〜1年と長期化した場合、レポート生成や Obsidian 出力にかかる時間とメモリ消費が増加する。
- **推奨対応**:
  - SQLite データベース（例: `data/health_data.db`）への蓄積テーブルを導入し、新規取り込み分のみを差分インサート、集計クエリで高速出力するアーキテクチャへの移行。

---

### 6. 【Low】ルートディレクトリのテスト・作業用スクリプト散在
- **対象**: プロジェクトルート
- **現状**:
  - `test_*.py`（約30ファイル）、`record_*.py`（約10ファイル）、`capture_*.py`（約8ファイル）がプロジェクト直下に散在しており、どれが本番コードでどれが調査用ワンオフスクリプトかが初見で判別しづらい。
- **推奨対応**:
  - 恒久的に必要なスクリプトを `scripts/` または `tests/` サブディレクトリへ移動・整理。
  - 不要となった試行錯誤用の一時ファイルをクリーンアップ。

---

### 7. 【Low】Obsidian出力先・個人環境依存パスの整理
- **対象ファイル**: [`config.py` L51-L54](file:///c:/KENKOU%20SYUTOKU/config.py#L51-L54)
- **現状**:
  - `OBSIDIAN_OUTPUT_DIR` のデフォルト値に `C:\Users\toshi\Dropbox\...` が残っています（`.env` で上書き可能）。
- **推奨対応**:
  - `.env.example` への `OBSIDIAN_KENKOU_DIR` の追記、およびデフォルト値が未設定の場合の挙動の共通化。

---

## 🎯 今後の推奨アクション順序

1. **第1弾（機能追加・安定化）**:
   - `collect_spo2()` の実装（4大指標 → 5大指標の完全自動化）
   - 体組成の画面復帰ロジックの改善（`launch_app()` またはトップ復帰確認処理の強化）
2. **第2弾（月跨ぎ対応）**:
   - カレンダー選択（ストレス）における月送り・年月判定の動的化
3. **第3弾（プロジェクト構造整理）**:
   - ルート直下の `test_*.py`, `record_*.py` のディレクトリ整理（`tests/`, `tools/`）
